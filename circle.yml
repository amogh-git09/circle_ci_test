machine:
  pre:
    - sudo pip install docker-compose
  services:
    - docker
  post:
    - docker-compose version

test:
  override:
    - docker-compose up
    - ./script.sh:
        parallel: true
