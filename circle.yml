machine:
  services:
    - docker
  post:
    - docker version

dependencies:
  pre:
    - sudo pip install docker-compose
  post:
    - docker-compose up

test:
  override:
    - ./script.sh:
        parallel: true
